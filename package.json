{
  "name": "starter",
  "version": "1.0.0",
  "description": "bare bones new mre app to build from",
  "main": "built/server.js",
  "scripts": {
    "clean": "tsc --build --clean",
    "build": "tsc --build",
    "start": "node .",
    "debug": "npm run build && nodemon --nolazy --inspect ."
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@microsoft/mixed-reality-extension-sdk": "^0.20.0",
    "dotenv": "^8.2.0"
  },
  "devDependencies": {
    "nodemon": "^2.0.4",
    "typescript": "^4.0.2"
  }
}
